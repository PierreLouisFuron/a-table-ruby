<%= form_with(model: recipe) do |form| %>
  <% if recipe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
        <% recipe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row mb-3">
    <div class="col-12"><%= form.text_field :name, class: "form-control rounded", placeholder: "Name" %></div>
  </div>

  <div class="form-group row mb-3">
    <div class="col-12"><%= form.text_area :description, rows: 7, class: "form-control rounded", placeholder: 'Description' %></div>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
