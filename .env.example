# Rails — required in production (signs/encrypts sessions, cookies, CSRF).
# Generate once with: openssl rand -hex 64
SECRET_KEY_BASE=XXXXXXXX

# DB CREDENTIALS (FOR DEV AND TEST ENVIRONMENTS)
POSTGRES_USER=XXXXXXXX
POSTGRES_PASSWORD=XXXXXXXX
